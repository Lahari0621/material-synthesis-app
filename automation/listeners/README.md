# Listeners

Pytest lifecycle listeners are implemented in `automation/conftest.py` so they
are loaded automatically. They capture final retry outcomes, failure
screenshots, stack traces, and device logcat for every catalog case.

