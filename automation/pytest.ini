[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    critical: release-blocking test
    high: high-priority test
    regression: regression coverage
    blocked: feature is not implemented in the application
filterwarnings =
    ignore::DeprecationWarning

